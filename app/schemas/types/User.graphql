type User {
  _id: ID
  firstName: String
  lastName: String
  email: String
  username: String
  role: Role

  photo: String
  bio: String
  status: UserStatus

  createdAt: DateTime
  updatedAt: DateTime

  followerCount: Int
}
